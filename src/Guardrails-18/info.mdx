# Reasoning Techniques
Guardrails provide the "Operational Boundary" for an agent. Instead of allowing an agent to execute any generated thought or tool call, Guardrails act as an interceptor layer that validates inputs and outputs against safety and business logic.

# Main Constraints 
1. Input Filtering 
2. Structural Validation
3. Hallucination Checkers

# How to use
1. Implement "Deterministic Checkpoints"
2. Enforce "Semantic Alignment"
3. The "Critic-Corrector" Loop: